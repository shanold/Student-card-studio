export function fileToDataUrl(file){return new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(r.result);r.onerror=()=>rej(r.error);r.readAsDataURL(file);});}
export async function normalizeImage(file,maxDimension=2400,quality=.9){const src=await fileToDataUrl(file),img=new Image();await new Promise((r,j)=>{img.onload=r;img.onerror=j;img.src=src}); const scale=Math.min(1,maxDimension/Math.max(img.width,img.height));const c=document.createElement('canvas');c.width=Math.round(img.width*scale);c.height=Math.round(img.height*scale);c.getContext('2d').drawImage(img,0,0,c.width,c.height);return c.toDataURL('image/jpeg',quality);}
